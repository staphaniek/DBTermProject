doctype html
html
  head
    meta(charset='utf-8')
  style.
    table {
      border: 1px solid #444444;
      border-collapse: collapse;
    }
    tr, td {
      border: 1px solid #444444;
    }
  body
    h1
      a(href='/home/'+user.id) Home
    h2
      a(href='/lecture') 내가 개설한 스터디
    artcle
      h2= 'Delete '+lecture.title+'?'
      form(action='/lecture/'+lecture.studyid+'/delete' method='post')
        p
          input(type='submit' value='YES')
      a(href='/lecture/') NO
