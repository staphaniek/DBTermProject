doctype html
html
  head
    meta(charset='utf-8')
  body
    h1
      a(href='/home/'+user.id) Home
    h2
      a(href='/lecture') 내가 개설한 스터디
    artcle
      form(action='/lecture/'+lecture.studyid+'/edit' method='post')
        p
          input(type='text' value=lecture.title placeholder='title' name='title')
        p
          textarea(name='description' placeholder='description')
            =lecture.description
        p
          div= 'by '+user.username
        p
          input(type='submit' value='수정')
