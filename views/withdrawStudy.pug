doctype html
html
  head
    meta(charset='utf-8')
  style.
    table {
      border: 1px solid #444444;
      border-collapse: collapse;
    }
    tr, td {
      border: 1px solid #444444;
    }
  body
    h1
      a(href='/home/'+user.id) Home
    h2
      a(href='/take') 내 신청 스터디
    artcle
      h2= 'Withdraw '+study.title+'?'
      form(action='/take/'+study.studyid+'/delete' method='post')
        p
          input(type='submit' value='YES')
      a(href='/take/') NO
